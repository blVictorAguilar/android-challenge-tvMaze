export enum Slices {
  FAVORITES = 'favorites',
  SHOWS = 'shows',
  RECENT_SEARCHES = 'recent_search',
}

export enum LoadingStatus {
  IDLE = 'idle',
  PENDING = 'pending',
  SUCCEEDED = 'succeeded',
  FAILED = 'failed',
}
